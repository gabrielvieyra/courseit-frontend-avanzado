// Components
import Title from "../Title/Title";

// Styles
import "./_navbar.scss";

const Navbar: React.FC = () => {
  return (
    <nav className="navbar">
      <input type="text" />
    </nav>
  );
};

export default Navbar;
